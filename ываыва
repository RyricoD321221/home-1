#include <stdio.h>


int max(int num1, int num2) {
return (num1 > num2) ? num1 : num2;
}

int main() {
int num1, num2;

if (scanf("%d %d", &num1, &num2) != 2) {
    printf("n/a");
    return 0;
}

printf("%d\n", max(num1, num2));

return 0;
}
